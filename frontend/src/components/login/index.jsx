import React, {Component} from 'react';
import { login } from '../../api/ApiCrudCliente'

export default class Login extends Component{
    render(){
        return(
            <p onClick={()=> listaClientes()}>oi</p>
        )
    }
}

